# The Pandas is an open source library providing data structures and data analysis
# tools in python.

# installing pandas :
# "pip install pandas" does the task, since pandas is a higher level library, it requires
# numpy(a lower level library) so it is installed first.

# enhanced Python interactive shell called IPython. IPython is just like the normal
# shell you get when you run python, but it provides better printingof large output.
# This makes IPython suitable for data analysis as it prints data in a well structured format.
# You can install IPython with pip:
#             pip install ipython
